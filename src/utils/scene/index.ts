/**
 * Internal utilities for scene manipulation.
 * These are NOT part of the public API and should only be used internally.
 * @internal
 */
export { deleteElementsFromMap, updateElementsInMap } from './elements';
export { deleteConnectionsForElements } from './connections';
