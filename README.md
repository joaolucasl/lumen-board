
# LumenBoard
# A simple whiteboard SDK for React


## Installation
1. Install dependencies:
   `pnpm install lumen-board`

2. Import the CSS in your root file (e.g., `main.tsx` or `App.tsx`):
   ```javascript
   import 'lumen-board/style.css';
   ```

3. Run the app:
   `pnpm run dev`
